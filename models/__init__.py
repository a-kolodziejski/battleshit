'''
Contains all models used and tested in the project.

agents.py contains the agent classes, which are responsible for selecting actions and learning from the environment.

neural_nets.py contains the neural network architectures used by the agents.
'''